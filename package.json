{
  "name": "root",
  "private": true,
  "license": "MIT",
  "npmClient": "yarn",
  "engines": {
    "node": "14 || 16"
  },
  "useWorkspaces": true,
  "workspaces": {
    "packages": [
      "integrations/*",
      "packages/*",
      "plugins/*"
    ]
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --stream --parallel --ignore @commonbridge/demo-*",
    "test": "jest --passWithNoTests",
    "lint": "lerna run lint --stream --parallel",
    "build": "lerna run build --ignore @commonbridge/demo-*"
  },
  "devDependencies": {
    "@types/jest": "^27.0.3",
    "@types/node": "^16.11.11",
    "jest": "^27.4.2",
    "lerna": "^4.0.0",
    "ts-jest": "^27.0.7",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2"
  }
}
